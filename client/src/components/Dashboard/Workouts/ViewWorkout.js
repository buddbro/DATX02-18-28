import React from 'react';
import {
  Text,
  View,
  ScrollView,
  TouchableOpacity,
  StyleSheet
} from 'react-native';

export default class ViewWorkout extends React.Component {
  constructor(props) {
    super(props);
  }
}
